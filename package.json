{
  "scripts": {
    "build": "npm run build --prefix frontend && dotnet publish api/api.csproj -c Release -o ./publish",
    "vercel-build": "npm run build",
    "postinstall": "npm install --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "create-react-app": "^5.1.0",
    "dotenv": "^17.2.1"
  }
}
